---
id: demo-2026-09-21-002
title: "Sample session note with no date (demo data)"
---

# Sample session note — intentionally broken

This record is missing its `date` field on purpose. RIDGE is
supposed to skip a record like this and log why, instead of
failing the whole run. This file exists so that behavior has
something real to point at.
